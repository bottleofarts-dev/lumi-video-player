# Video Player App

A modern video player web application built with React, Tailwind CSS, and Capacitor. 
It supports native deployment to Android via GitHub actions.

## Building APK natively
When you push to the `main` branch on GitHub, the GitHub Action automatically runs to build the `.apk` file using Capacitor. You can find the resulting APK file in the "Actions" tab of your GitHub repository.
